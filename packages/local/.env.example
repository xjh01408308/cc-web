# ===== local 配置 =====
# 复制此文件为 .env 后修改: cp .env.example .env

# 环境模式: development (默认) 或 production
NODE_ENV=production

# 中转服务 WebSocket 地址
# 同机器部署（推荐）: ws://127.0.0.1:3001/ws/local（本机回环，无需 TLS）
# 跨机器部署（需要 nginx 代理 WSS）: wss://your-domain.com/ws/local
RELAY_URL=ws://127.0.0.1:3001/ws/local
# 注册认证 token（须与 packages/relay/.env 中的 RELAY_TOKEN 一致）
# ⚠️ 生产环境必须与 relay 端使用相同的强随机值
RELAY_TOKEN=<与 relay 端 RELAY_TOKEN 一致>
# 中继服务 TLS CA 证书路径（wss 连接自签名证书时使用，留空则使用系统 CA）
RELAY_CA_CERT=
# 节点标识（留空则自动使用本机 hostname）
NODE_ID=
# 节点登录密码（留空则不启用密码认证）
# ⚠️ 公网部署时强烈建议设置此值
NODE_PASSWORD=
# 项目工作区根目录（留空则不限制路径）
# 设置后，create_project 和 create_session 的路径必须在此目录下
# 例如: WORKSPACE_ROOT=C:/projects
WORKSPACE_ROOT=
# WebSocket 重连初始延迟（毫秒）
RECONNECT_DELAY=2000
# WebSocket 重连最大延迟（毫秒）
MAX_RECONNECT_DELAY=30000
# 强制锁定权限模式，忽略前端传的值（留空 = 以前端为准）
# 支持的模式: bypassPermissions | acceptEdits | default | plan
# ⚠️ 生产环境下，如需 bypassPermissions 必须通过此变量显式启用
CLAUDE_FORCE_PERMISSION_MODE=
