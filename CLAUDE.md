# CLAUDE.md

Repository-level guidance for Claude Code working in `cc-web`.

## Agent skills

### Issue tracker

Issues and PRDs live as GitHub issues via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical triage roles; each label string equals its name (defaults). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context — one `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
